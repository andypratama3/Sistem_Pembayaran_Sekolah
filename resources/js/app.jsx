import React from 'react';
import { createRoot } from 'react-dom/client';
import '../css/app.css';

// TemplateEditor removed — no client-side mounts are required.

window.React = React;
window.createRoot = createRoot;
